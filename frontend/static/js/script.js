// Currently no page-level JS is needed — the Login buttons link straight to
// the /login page. Keep this file as the place to add interactivity
// (e.g. client-side validation, dashboard behavior) as the backend grows.
